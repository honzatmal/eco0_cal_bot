name: Economic Calendar Bot

on:
  schedule:
    # 매일 한국 시간 07:05 (UTC 22:05) 에만 실행
    - cron: '5 22 * * *'
  workflow_dispatch: # 수동 테스트용 버튼

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  run_bot:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

    - name: Install Dependencies
        run: |
          pip install python-telegram-bot requests beautifulsoup4 yfinance

      # Secrets 설정 확인 (TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID)
      - name: Execute Script
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: python main.py
